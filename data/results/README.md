Result assets:
- Primary run log is provided as `transfer_3trial_results.json` (324 runs, 1,512 turns).
- Integrity hash is provided in `checksums_sha256.txt`.
- Local reruns can be generated with `experiments/run_transfer_3trial.py`.
